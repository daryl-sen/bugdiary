import axios from "axios";
import { useEffect, useState } from "react";

export default function useDiaryData() {
  const [diaryData, setDiaryData] = useState();

  // useEffect(() => {
  //   axios.
  // }, [diaryData]);
}
